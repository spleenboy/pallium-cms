ul.pure-menu-list
    li.pure-menu-heading Entries
    each type, key in entries.types
        if key === params.type
            li.pure-menu-item.pure-menu-selected
                a.pure-menu-link(href="/entry/#{key}/list")= type.plural
        else
            li.pure-menu-item
                a.pure-menu-link(href="/entry/#{key}/list")= type.plural
